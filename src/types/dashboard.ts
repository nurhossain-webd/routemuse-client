import type { Experience } from "./experience";
export interface DashboardSummary { statistics: { experiencesCreated: number; favorites: number; reviewsSubmitted: number; aiPlansGenerated: number; estimatedPlanSpending: number }; categoryInteractionDistribution: { category: string; count: number }[]; monthlyPlanningActivity: { month: string; plans: number; estimatedSpending: number }[]; recentPlans: { _id: string; title: string; destination: string; startDate: string; endDate: string; estimatedTotal: number }[]; recentSavedExperiences: { _id: string; experience: Experience; createdAt: string }[]; recentInteractions: { _id: string; type: string; createdAt: string; experience: Pick<Experience, "_id" | "title" | "slug" | "category" | "location" | "imageUrls"> | null }[] }
